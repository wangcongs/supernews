version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    environment:
      - DATABASE_URL=sqlite:///./dev.db
  frontend:
    build: ./frontend
    ports:
      - "3000:5173"
    depends_on:
      - backend
